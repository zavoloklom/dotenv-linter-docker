pre-commit:
  parallel: true
  commands:
    hadolint:
      glob: "*Dockerfile*"
      run: task lint:hadolint -- {staged_files}
    markdownlint:
      glob: "*.md"
      exclude: "CHANGELOG.md"
      run: task lint:markdownlint -- {staged_files}
